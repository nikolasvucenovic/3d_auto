{
  "name": "3d-auto",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Offline text-to-Blender scene and animatic workstation",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test",
    "benchmark": "node src/evaluation-runner.mjs --planner both",
    "benchmark:baseline": "node src/evaluation-runner.mjs --planner deterministic",
    "audit:bundle": "node installer/modules/verify-catalog.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
